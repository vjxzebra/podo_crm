# Матриця трасування критеріїв готовності Podoria CRM

- Версія: `0.1`
- Статус: базова матриця етапу 0
- Джерело вимог: [`SPECIFICATION.md`](../../SPECIFICATION.md), розділ 21
- Візуальне джерело: [`design/index.html`](../../design/index.html)
- Пов’язана UI-карта: [`screen-state-access-map.md`](screen-state-access-map.md)

## Призначення

Цей документ зв’язує всі 23 критерії готовності зі специфікацією, вертикальною фічею, цільовим API-контрактом, обов’язковими станами інтерфейсу та майбутніми автоматизованими тестами.

Матриця є індексом, а не заміною детальних task packets, ADR, OpenAPI-схеми або тест-кейсів. До появи реалізації всі рядки мають стан `planned`. Після реалізації стан змінюється лише за наявності посилань на код і зелені автоматизовані докази.

## Правила трасування

- ID `AC-01`—`AC-23` стабільні та відповідають нумерації розділу 21 специфікації.
- Пріоритет джерел: `SPECIFICATION.md` → погоджені ADR → прототип → демонстраційний JavaScript.
- Назви API нижче є цільовими operation/route contracts для подальшої фіксації в OpenAPI; вони ще не означають наявність endpoint у коді.
- Усі mutation-відповіді використовують спільний error envelope: `code`, `message`, `fields`, `correlation_id`.
- `401` означає відсутню/прострочену сесію, `403` — заборонену роль або scope, `404` може приховувати існування чужого об’єкта, `409` — конфлікт стану або конкурентної зміни, `422` — помилку полів.
- Гроші передаються цілим числом у копійках; час — ISO 8601 з UTC на API та `Europe/Kyiv` у UI.
- Критичні повторювані mutation (`finish_visit`, оплата, повернення, складська операція) мають ідемпотентний ключ.
- Для кожного рольового критерію потрібні позитивний тест дозволеної ролі та негативні тести заборонених ролей.

## Матриця

| ID | Критерій готовності | Джерело у специфікації | Вертикальна фіча / етап | Цільовий API-контракт | Обов’язкові UI-стани й орієнтир прототипу | Майбутній автоматизований доказ | Стан / відкриті рішення |
|---|---|---|---|---|---|---|---|
| AC-01 | Кожна роль бачить лише дозволені розділи й дані. | §2.1–2.4, §4, §5, §7.1, §9, §11–17 | Auth/RBAC та role-scoped selectors; етап 2, далі regression gate кожного домену | `GET /api/v1/session`; усі list/detail endpoints застосовують role policy і object scope до serialization | Role-aware sidebar/mobile menu; дозволений огляд; приховані пункти; direct URL → безпечний redirect; 401/403/404; прототип: `.nav-item`, `data-admin-only`, `data-finance-access` | `backend/tests/security/test_rbac_matrix.py`; `frontend/e2e/ac-01-rbac.spec.ts`: 3 role storage states, навігація, прямі URL, IDOR і відсутність заборонених JSON-полів | `planned`; матриця полів reception/medical буде деталізована в task packets |
| AC-02 | Ресепшн та адмін можуть створити запис, а подолог — запис до себе. | §2.1–2.3, §6.4–6.7 | Appointment create; етап 4 | `GET /api/v1/appointments/availability`; `POST /api/v1/appointments`; для podologist сервер ігнорує/відхиляє чужий `specialist_id` | Новий запис із header, вільного слота й mobile CTA; podologist бачить себе як незмінного спеціаліста; loading, success, 422, 403; прототип: `#newAppointment`, booking modal | `backend/apps/scheduling/tests/api/test_create_appointment.py`; `frontend/e2e/ac-02-create-appointment.spec.ts` для трьох ролей | `planned` |
| AC-03 | Неможливо створити запис на зайнятий час того самого спеціаліста. | §1.2, §6.2, §6.7 | Availability + DB conflict protection; етап 4 | `POST /api/v1/appointments` і `PATCH /api/v1/appointments/{id}` → `409 appointment_slot_conflict`; PostgreSQL exclusion constraint на specialist/time range | Попередня availability-перевірка; серверний conflict state без втрати введених даних; пропозиція вибрати інший час | `backend/apps/scheduling/tests/test_overlap_constraint.py`; concurrency test із двома одночасними POST; `frontend/e2e/ac-03-busy-slot.spec.ts` | `planned`; ADR про конфлікт кімнат не змінює обов’язковий specialist constraint |
| AC-04 | Записи різних спеціалістів на один час коректно відображаються один під одним. | §6.1–6.3, §19 | Calendar day/week read model; етап 4 | `GET /api/v1/calendar?from=&to=&specialist_id=` повертає окремі events зі стабільними `starts_at`, `ends_at`, `specialist`, `room`, `status` | Денний календар: паралельні картки не перекривають текст; tablet/mobile scroll/column states; прототип: calendar events і responsive CSS | Selector test для однакового часу різних спеціалістів; `frontend/e2e/ac-04-concurrent-layout.spec.ts` + screenshots desktop/tablet/phone | `planned`; кімната очікує ADR-001 |
| AC-05 | Пацієнта можна знайти або створити без виходу з форми запису. | §6.4–6.5, §7.2–7.3 | Patient picker + inline create; етапи 3–4 | `GET /api/v1/patients?search=`; `POST /api/v1/patients`; відповідь create одразу придатна як appointment patient reference | Picker: idle, typing, loading, results, empty, duplicate-phone warning, inline create, validation, selected patient; booking modal не закривається | `backend/apps/patients/tests/api/test_search_and_create.py`; `frontend/e2e/ac-05-inline-patient.spec.ts` | `planned` |
| AC-06 | Запис із картки пацієнта не дозволяє змінити цього пацієнта. | §6.4–6.5, §7.4 | Patient-context appointment create; етап 4 | Той самий `POST /api/v1/appointments`; UI route context передає `patient_id`, а сервер перевіряє доступ до пацієнта | У формі показано locked patient identity; picker відсутній/disabled; cancel не змінює контекст; прототип: appointment form opened from patient card | Component test locked field; `frontend/e2e/ac-06-locked-patient.spec.ts`; API IDOR test на чужий `patient_id` | `planned` |
| AC-07 | Скарги можна заповнити або явно вказати їх відсутність. | §6.6, §8.1 | Appointment complaint invariant + visit examination; етапи 4 і 6 | Appointment/visit schemas мають `complaints` і `has_no_complaints`; правило: непорожній текст XOR явна відсутність; invalid → `422 complaints_required` | Textarea; toggle «Скарг немає»; disabled/optional text state; field error; значення зберігаються після server error | Serializer/service parametrized tests; component form test; `frontend/e2e/ac-07-complaints.spec.ts` | `planned` |
| AC-08 | Подолог може оформити всі чотири кроки прийому. | §2.1, §8.1–8.7 | Visit wizard and draft; етап 6 | `GET /api/v1/visits/{id}`; `PUT /api/v1/visits/{id}/draft`; `POST /api/v1/visits/{id}/finish`; лише assigned podologist/admin | 4 кроки: examination, services/materials, photos, finish; back/next; autosave/manual draft; loading, validation, unsaved, retry; прототип: `#visitWorkspace`, `[data-visit-panel]` | Service/API tests усіх кроків і role scope; component tests navigation/draft; `frontend/e2e/ac-08-visit-wizard.spec.ts` | `planned` |
| AC-09 | Послуги й матеріали мають пошук, а послуги — кількість. | §8.2, §18 | Visit line editor; етап 6, залежить від довідників і складу | `GET /api/v1/services?search=&active=true`; `GET /api/v1/inventory/materials?search=`; `GET /api/v1/inventory/materials/{id}/lots?usable=true`; visit draft lines містять integer service quantity та decimal material quantity | Search idle/loading/results/empty; service quantity ±/input; повторне додавання збільшує quantity; lot/expiry/available unit; insufficient-stock error | Unit tests totals/dedup/quantity; API search tests; `frontend/e2e/ac-09-visit-lines.spec.ts` | `planned` |
| AC-10 | Фото однозначно додаються до блоку «До» або «Після» конкретного відвідування. | §7.7, §8.3 | Private visit photos; етап 6 | `POST /api/v1/visits/{id}/photos/upload-intents`; `POST /api/v1/visits/{id}/photos` з enum `kind=before|after`; `DELETE .../{photo_id}` до finish; authorized signed read URL | Окремі before/after dropzones; uploading/progress, preview, invalid file, retry, delete-before-finish, empty block; прототип: `[data-visit-photo=before|after]` | Model/API tests visit ownership and kind; private-object/IDOR tests; `frontend/e2e/ac-10-visit-photos.spec.ts` | `planned`; формати, ліміт і retention очікують ADR-004 |
| AC-11 | Завершення прийому створює історію, списує матеріали та передає повну суму на оплату. | §8.4–8.7, §9.2, §11.5 | Atomic `finish_visit`; етап 6 | `POST /api/v1/visits/{id}/finish` з `Idempotency-Key`; в одній транзакції створює completed visit/history, stock movements, receivable, audit і optional follow-up; response повертає IDs і total_minor | Review summary; submit disabled until valid; submitting; success/deep links; recoverable 409 stock/slot conflict; retry без дублювання | Transaction rollback test із fault injection; idempotency test; totals/ledger assertions; `frontend/e2e/ac-11-finish-visit.spec.ts` | `planned`; критична транзакція потребує QA/security review |
| AC-12 | Наступний запис можна створити тільки на вільний час. | §6.7, §8.5–8.6 | Follow-up within visit completion; етап 6 | Availability query як AC-02; optional `next_appointment` у `finish_visit`; stale/busy slot → atomic `409 appointment_slot_conflict` без завершення visit | Toggle follow-up; date/specialist/service; free, busy, no-slots, loading; selected slot; stale-conflict recovery; прототип: `#followupBookingCard`, `#followupTimeSlots` | Availability/service tests; atomic rollback when follow-up conflicts; `frontend/e2e/ac-12-follow-up-slot.spec.ts` | `planned` |
| AC-13 | Часткова оплата ніде не пропонується. | §1.3, §9.2, §20 | Receivable/full payment invariant; етап 7 | `POST /api/v1/payments` приймає `visit_id`, method, comment; amount server-derived and must equal unpaid total; no partial-payment endpoint/schema | Read-only full total; only full-payment confirmation; no custom amount or partial status in desktop/mobile; server conflict if already paid | OpenAPI schema assertion (amount absent/readonly); service invariant tests; `frontend/e2e/ac-13-no-partial-payment.spec.ts` | `planned` |
| AC-14 | Ресепшн може провести оплату, повернення, внесення та вилучення готівки. | §2.2, §9.1–9.7, §10.1–10.2 | Finance ledger operations; етап 7 | `POST /api/v1/payments`; `POST /api/v1/payments/{id}/refunds`; `POST /api/v1/cash-movements`; open-shift precondition; mutation keys idempotent | Finance list + payment/refund/deposit/withdrawal modals; patient/payment search; confirmation, success, shift-required, insufficient-cash, already-paid states | Ledger/service/API tests per operation and role; idempotency tests; `frontend/e2e/ac-14-finance-operations.spec.ts` | `planned`; full vs partial refund очікує ADR-003, safe default — only full refund |
| AC-15 | Внесення й вилучення не містять полів пацієнта та способу оплати. | §9.5–9.6 | Cash adjustment contract/UI; етап 7 | `POST /api/v1/cash-movements` schema: `type=deposit|withdrawal`, `amount_minor`, `reason`, `comment`; additional properties rejected; no patient/payment method fields | Dedicated deposit/withdrawal forms with amount, reason, optional comment only; withdrawal shows available cash | OpenAPI negative schema tests; component assertion fields absent; `frontend/e2e/ac-15-cash-adjustment-fields.spec.ts` | `planned` |
| AC-16 | Ресепшн може закрити власну касову зміну після звірки. | §10.1–10.4 | Cash-shift reconciliation; етап 7 | `GET /api/v1/cash-shifts/current`; `POST /api/v1/cash-shifts/{id}/close` з actual cash, counted confirmation, comment; owner/admin authorization; ledger-derived expected cash | Summary, unpaid warning, expected/actual, live discrepancy, counted checkbox, required discrepancy comment, disabled submit, success | Service tests owner/admin/other reception; discrepancy validation; concurrent close/idempotency; `frontend/e2e/ac-16-close-own-shift.spec.ts` | `planned` |
| AC-17 | Історія касових змін відкриває повний список операцій вибраної зміни. | §10.5 | Cash-shift history/details; етап 7 | `GET /api/v1/cash-shifts?from=&to=&employee_id=`; `GET /api/v1/cash-shifts/{id}` includes paginated/complete operations contract and derived totals | History loading/empty/filter; row selection; detail modal/list, totals, discrepancy; прототип: `.shift-history-row`, cash shift details modal | Selector totals test; API detail completeness test; `frontend/e2e/ac-17-shift-history.spec.ts` | `planned` |
| AC-18 | Завершення прийому автоматично створює складські рухи. | §8.6, §11.5 | Visit-to-inventory transaction; етап 6 | Той самий atomic `finish_visit`; append-only `StockMovement` per material/lot with visit, patient, podologist, quantity, timestamp; lot rows locked | Finish success updates visit history; admin stock journal shows linked auto write-offs; draft does not affect stock | Transaction test movement fields/balances; concurrency and rollback; draft-no-side-effects test; `frontend/e2e/ac-18-auto-stock-movement.spec.ts` | `planned` |
| AC-19 | Адмін може провести надходження, ручне списання та інвентаризацію. | §11.1–11.8 | Inventory operations; етап 5 | `POST /api/v1/inventory/receipts`; `POST /api/v1/inventory/write-offs`; `POST /api/v1/inventory/stocktakes/{id}/post`; row locks, checks, append-only movements | Receipt multi-line/search; manual write-off available/quantity/reason; stocktake system-vs-actual/difference/confirm; loading/error/success; прототип: `#stockReceiptModal`, `#manualWriteoffModal`, `#inventoryModal` | API/service tests, no-negative-stock concurrency, expired-lot and immutability tests; `frontend/e2e/ac-19-inventory-operations.spec.ts` | `planned` |
| AC-20 | Усі критичні зміни фіксуються в журналі дій із деталями «Було → Стало». | §6.8, §14.1–14.3, перелік mutation у доменах | Append-only audit infrastructure; етап 2, інтегрується в кожен наступний етап | Domain services emit audit in same transaction; `GET /api/v1/audit-events` і `GET /api/v1/audit-events/{id}` admin-only; before/after snapshots redact secrets | Audit loading/empty/search/filter/list/detail; before/after, actor, object, result; прототип: `#auditView`, `.audit-row` | Per-domain audit assertion; rollback creates no event; immutable model test; redaction and admin-only API/e2e; `frontend/e2e/ac-20-audit.spec.ts` | `planned`; перелік критичних event types стане окремим registry |
| AC-21 | Пароль можна встановити, змінити та відновити через адміна. | §3.1–3.4, §12.2–12.3, §14.1 | Account lifecycle; етап 2 | `POST /api/v1/auth/first-login-password`; `POST /api/v1/auth/change-password`; `POST /api/v1/password-reset-requests`; admin `POST /api/v1/users/{id}/temporary-password`; session rotation/revocation policy | Login; first-login blocking modal; own-password form; forgot request success without account enumeration; admin request queue/temp password; прототип: `#loginScreen`, `#firstLoginModal`, `#forgotPasswordModal` | Password policy/service tests; enumeration-safe API test; forced-first-login/session tests; `frontend/e2e/ac-21-password-lifecycle.spec.ts` | `planned`; notification channel for temp password must be decided in auth task packet |
| AC-22 | Глобальний пошук і сповіщення дотримуються рольових обмежень. | §2.4, §15, §16 | Scoped search + notifications; етап 8 | `GET /api/v1/search?q=` returns only allowed categories/objects; `GET /api/v1/notifications`; `POST .../{id}/read`; `POST .../read-all`; policies applied before serialization | Search typing/loading/grouped/empty/deep link and mobile fullscreen; notification all/unread/count/read-all/deep link; прототип: `#globalSearchPanel`, `#notificationPanel` | Cross-role leakage/IDOR API tests with seeded forbidden matches; `frontend/e2e/ac-22-search-notifications-rbac.spec.ts` | `planned` |
| AC-23 | Основні сценарії працюють на комп’ютері, планшеті й телефоні. | §2.4, §4, §19 | Responsive shell and cross-feature UAT; етапи 1 і 9, gate кожної UI feature | API однаковий для всіх viewport; no device-specific authorization/business behavior | Desktop sidebar/topbar; tablet compact rail; mobile bottom nav/fullscreen sheets; keyboard/focus/touch/loading/error/unsaved states; прототип previews: `mobile-*`, `tablet-calendar`, responsive CSS | Playwright projects: desktop `1440×900`, tablet `768×1024`, phone `390×844`; critical journey AC-02→05→08→11→14→16 plus visual/accessibility checks | `planned`; точні browser/version targets фіксуються в QA strategy |

## Покриття за етапами

| Етап | Критерії, що вперше закриваються | Критерії, які регресійно перевіряються |
|---|---|---|
| 0. Вимоги й ADR | Матриця для AC-01—AC-23 | — |
| 1. Платформа й shell | Базова інфраструктура AC-23 | Error envelope, test harness, responsive shell |
| 2. Auth/RBAC/довідники | AC-01, AC-21; інфраструктура AC-20 | AC-23 |
| 3. Пацієнти й справи | Передумови AC-05—AC-06 | AC-01, AC-20, AC-23 |
| 4. Календар і записи | AC-02—AC-07 | AC-01, AC-20, AC-23 |
| 5. Склад | AC-19; передумови AC-09, AC-11, AC-18 | AC-01, AC-20 |
| 6. Оформлення прийому | AC-08—AC-12, AC-18 | AC-01, AC-03, AC-07, AC-19, AC-20, AC-23 |
| 7. Фінанси й каса | AC-13—AC-17 | AC-01, AC-11, AC-20, AC-23 |
| 8. Пошук/сповіщення/аудит/аналітика | AC-22; повний UI AC-20 | Усі role/ledger-sensitive критерії |
| 9. UAT і запуск | Фінальний AC-23 | AC-01—AC-23 |

## Обов’язкові gate-артефакти

Для переведення будь-якого рядка з `planned` у `implemented` потрібно додати:

1. посилання на task packet і, якщо потрібно, погоджений ADR;
2. OpenAPI operation та приклади success/error;
3. backend unit/integration тести, включно з negative RBAC;
4. frontend component test для validation/state logic;
5. Playwright-сценарій для user-visible acceptance criterion;
6. посилання на реалізацію audit event для mutation;
7. докази desktop/tablet/phone для користувацького екрана;
8. результат Docker test profile у CI.

Стани рядка:

- `planned` — джерело, цільовий контракт і докази визначені, реалізації ще немає;
- `in_progress` — є task packet і активна реалізація;
- `implemented` — код і локальні тести готові;
- `verified` — інтеграційний gate і пов’язаний acceptance test пройдені;
- `blocked` — потрібне рішення ADR або зовнішня залежність; причина має бути в останній колонці.
